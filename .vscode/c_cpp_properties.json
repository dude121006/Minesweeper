{

    "version": 4,
    "configurations": [
        {
            "name": "Windows",
            "intelliSenseMode": "gcc-x64",
            "includePath": [
                "${workspaceFolder}/include/",  // Path to your include folder
                "${workspaceFolder}/src/depend/stb_image/*",  // Path to your include folder
                "${workspaceFolder}/include/*",  // Path to your include folder
                "${workspaceFolder}/include/**",  // Path to your include folder
                "C:/MinGW/include"  // Add the MinGW include path
            ]
        }
    ]        
}